#include "stdio.h"
int main(int x,int y)
{
1 && (!(1 + 0));
12 + (98721 + 36265 / 456 ) * (0 + 1687);
x=x+1*x+89;

//char xi='a';

printf("%d", x+x*2, x+y);
for(int k=0;k<5;k=k+1)
    {

    x=x+1;
    }
return 1;
}